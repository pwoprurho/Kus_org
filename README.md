Kusmus Data Sprint v5 - Quickstart

Run `python -m pip install -r requirements.txt`
Then run `python run_sample.py`. On first run a .env will be created from .env.example. Edit .env to add GEMINI_API_KEY and re-run.
