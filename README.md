# Kusmus Data Sprint v5 (Full Gemini Version)
Run `pip install python-genai python-dotenv pandas datasets`
Add GEMINI_API_KEY to `.env` and run:
```
python generate_kusmus_data_gemini_v5.py
```
Outputs in `output/`.
